version: "3.8"

services:
  mysql:
    image: mysql:8.0
    container_name: anonim-chat-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: "141722A!"
      MYSQL_DATABASE: anonim_chat
      MYSQL_CHARSET: utf8mb4
      MYSQL_COLLATION: utf8mb4_unicode_ci
      TZ: Europe/Moscow
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci --default-time-zone=+03:00
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-p141722A!"]
      interval: 5s
      timeout: 5s
      retries: 10

  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    container_name: anonim-chat-pma
    restart: always
    environment:
      PMA_HOST: mysql
      PMA_PORT: 3306
      UPLOAD_LIMIT: 64M
    ports:
      - "8080:80"
    depends_on:
      mysql:
        condition: service_healthy

  bot-ru:
    build: .
    container_name: anonim-chat-bot-ru
    restart: always
    env_file:
      - .env
    environment:
      BOT_LANG: ru
      DB_HOST: mysql
      DB_PORT: 3306
      DB_USER: root
      DB_PASSWORD: "141722A!"
      DB_NAME: anonim_chat
      TZ: Europe/Moscow
    depends_on:
      mysql:
        condition: service_healthy

  bot-en:
    build: .
    container_name: anonim-chat-bot-en
    restart: always
    environment:
      BOT_TOKEN: "8375919305:AAHOF3kHhffa7F-0VAOes9-03lx3vyBZQMM"
      BOT_USERNAME: "en_anonim_chat_bot"
      BOT_LANG: en
      DB_HOST: mysql
      DB_PORT: 3306
      DB_USER: root
      DB_PASSWORD: "141722A!"
      DB_NAME: anonim_chat
      TZ: Europe/Moscow
    depends_on:
      mysql:
        condition: service_healthy

volumes:
  mysql_data:
