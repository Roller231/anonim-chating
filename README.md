# Анонимный чат-бот для Telegram 🔵

Бот для анонимного общения в Telegram с поиском по полу, возрасту и стране.

## Функционал

- `/start` — регистрация (анкета) или начало поиска собеседника
- `/stop` — завершение чата + оценка собеседника (лайк/дизлайк)
- `/next` — следующий собеседник (завершает текущий чат и ищет нового)
- `/search` — настройки предпочтений поиска (пол, возраст, страна)
- `/lnk` — отправить ссылку на свой профиль собеседнику
- `/ref` — реферальный кабинет
- `/exchange` — обмен реферальных баллов на VIP
- `/top` — рейтинги (по карме, рефералам, активности)

## Стек

- **Python 3.11+**
- **aiogram 3.x** — async Telegram Bot API
- **SQLAlchemy 2.0 async** + **aiomysql** — MySQL ORM
- **Redis** — быстрая очередь поиска собеседников
- **Alembic** — миграции БД

## Установка

```bash
# 1. Клонировать репозиторий
git clone <repo_url>
cd anonim-chat

# 2. Создать виртуальное окружение
python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Linux/Mac

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Настроить переменные окружения
copy .env.example .env
# Отредактировать .env — указать BOT_TOKEN, настройки MySQL и Redis

# 5. Создать базу данных MySQL
mysql -u root -p -e "CREATE DATABASE anonim_chat CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# 6. Запустить бота (таблицы создаются автоматически)
python -m bot.main
```

## Структура проекта

```
bot/
├── config.py          # Конфигурация из .env
├── main.py            # Точка входа
├── db/
│   ├── engine.py      # SQLAlchemy engine + session pool
│   ├── models.py      # ORM модели (User, Chat, Rating, etc.)
│   └── repositories.py # Репозитории для работы с БД
├── handlers/
│   ├── start.py       # /start + регистрация
│   ├── chat.py        # /stop, /next, /lnk + пересылка сообщений
│   ├── search.py      # /search — настройки предпочтений
│   ├── referral.py    # /ref, /exchange
│   └── top.py         # /top — рейтинги
├── keyboards/
│   └── inline.py      # Inline-клавиатуры
├── middlewares/
│   └── db.py          # DB session middleware
├── services/
│   ├── matching.py    # Redis-очередь поиска
│   └── chat.py        # Логика чатов
└── states/
    └── registration.py # FSM состояния
```

## Сущности БД

| Таблица | Описание |
|---------|----------|
| `users` | Пользователи: профиль, статистика, VIP, предпочтения |
| `user_interests` | Интересы пользователей |
| `chats` | Чаты между пользователями |
| `search_queue` | Очередь поиска (fallback для Redis) |
| `ratings` | Оценки после чатов (лайк/дизлайк) |
| `referrals` | Реферальные связи |
